name: Trigger auto deployment for container-app-o5s5mkjle535w

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ space3 ]
    paths:
    - 'server/**'
    - '.github/workflows/container-app-o5s5mkjle535w-AutoDeployTrigger-b115cc09-8225-4335-83a5-6d7c92be5cf1.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPO5S5MKJLE535W_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPO5S5MKJLE535W_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPO5S5MKJLE535W_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}/server 
          registryUrl: containerregistryo5s5mkjle535w.azurecr.io
          registryUsername: ${{ secrets.CONTAINERAPPO5S5MKJLE535W_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPO5S5MKJLE535W_REGISTRY_PASSWORD }}
          containerAppName: container-app-o5s5mkjle535w
          resourceGroup: todoa237
          imageToBuild: containerregistryo5s5mkjle535w.azurecr.io/container-app-o5s5mkjle535w:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


