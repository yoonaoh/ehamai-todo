name: Trigger auto deployment for container-app-rs24i7c7ivk5u

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ space7 ]
    paths:
    - 'server/**'
    - '.github/workflows/container-app-rs24i7c7ivk5u-AutoDeployTrigger-d12c0295-ad22-4957-9131-1a1efb688e80.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPRS24I7C7IVK5U_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPRS24I7C7IVK5U_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPRS24I7C7IVK5U_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}/server 
          registryUrl: containerregistryrs24i7c7ivk5u.azurecr.io
          registryUsername: ${{ secrets.CONTAINERAPPRS24I7C7IVK5U_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPRS24I7C7IVK5U_REGISTRY_PASSWORD }}
          containerAppName: container-app-rs24i7c7ivk5u
          resourceGroup: todo94be
          imageToBuild: containerregistryrs24i7c7ivk5u.azurecr.io/container-app-rs24i7c7ivk5u:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


