name: Trigger auto deployment for container-app-o2amev2m25fcg

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - 'server/**'
    - '.github/workflows/container-app-o2amev2m25fcg-AutoDeployTrigger-ec24ebe0-d960-4912-846e-08de13f5626e.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPO2AMEV2M25FCG_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPO2AMEV2M25FCG_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPO2AMEV2M25FCG_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}/server 
          registryUrl: containerregistryo2amev2m25fcg.azurecr.io
          registryUsername: ${{ secrets.CONTAINERAPPO2AMEV2M25FCG_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPO2AMEV2M25FCG_REGISTRY_PASSWORD }}
          containerAppName: container-app-o2amev2m25fcg
          resourceGroup: ehamaitodo807c
          imageToBuild: containerregistryo2amev2m25fcg.azurecr.io/container-app-o2amev2m25fcg:${{ github.sha }}
          dockerfilePath: Dockerfile


